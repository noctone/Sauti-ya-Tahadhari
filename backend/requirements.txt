# Core — always required
pydantic>=2.0
fastapi>=0.110.0
uvicorn>=0.29.0

# LLM providers — install ONLY the one(s) matching your LLM_PROVIDER choice.
# Installing all three is fine but not necessary.
anthropic>=0.40.0          # required if LLM_PROVIDER=anthropic
google-generativeai>=0.8.0 # required if LLM_PROVIDER=gemini
openai>=1.40.0             # required if LLM_PROVIDER=openai
