# Copy to .env.local. Points the frontend at your running backend.
# Local dev (uvicorn running on default port):
NEXT_PUBLIC_API_URL=http://localhost:8000

# When you deploy the backend (Render/Railway), change this to that public URL instead.
